import React from "react";
import Leaderboard from "leaderboard";

const SumraLeaderboard = () => {
    return (
        <div className="sumra-referrals-main">
            <Leaderboard />
        </div>
    );
};

export default SumraLeaderboard;
